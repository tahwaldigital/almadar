# نقد شامل لتصميم تطبيق «المدار نيوز» وخطة تطوير متكاملة

> إعداد: مراجعة خبير تصميم تطبيقات الأخبار · التاريخ: 2026-06-02 · النطاق: جميع شاشات التطبيق (Flutter / عربي RTL)
> المنهجية: فحص كود 24 شاشة ومكوّناً + نظام التصميم (الألوان، الخطوط، المسافات، الثيم) قراءةً مباشرة، ثم تحليلها بمعايير تطبيقات الأخبار الرائدة.

---

## أولاً: الخلاصة التنفيذية

التطبيق يقف على أساس هندسي ممتاز (Clean Architecture، Riverpod، حالات تحميل/خطأ، دعم RTL ووضع داكن، توكنات `AppColors`/`AppTypography`). لكن من منظور **التصميم البصري وتجربة القراءة**، التطبيق ما زال في مرتبة «وظيفي ومقبول» وليس «صحيفة رقمية راقية». المشكلات ليست أعطالاً، بل غياب لغة تصميم موحّدة وحاسمة: لوحة ألوان متذبذبة بين البارد والدافئ، تدرّج طباعي ضعيف، حالات فارغة/خطأ غير متّسقة، نقص في التغذية الراجعة اللمسية والحركة، وأخطاء طباعة عربية دقيقة. هذا المستند يفكّك كل ذلك ويضع خطة تطوير على أربع مراحل.

**التقييم العام:** البنية 9/10 · التصميم البصري 5.5/10 · تجربة القراءة 6/10 · الاتساق 5/10 · إمكانية الوصول 4/10.

---

## ثانياً: النقد حسب المحاور

### 1) نظام الألوان — المشكلة الأعمق
لوحة الأسطح باردة ومائلة للأزرق (`surface #F8F9FF`, `surfaceContainer #E5EEFF`, `surfaceContainerHigh #DCE9FF`)، بينما الأكسنت دافئ برتقالي (`primary #9D4300`, `primaryContainer #F86F05`)، واللون الثانوي رمادي-أزرق (`secondary #4A5F81`). هذا المزج بين عائلتين حراريتين (أزرق بارد + برتقالي دافئ) بلا جسر لوني يخلق إحساساً «غير محسوم». الأخطر: لون نص المتن `onSurfaceVariant #594236` **بنّي**، يُستخدم فوق سطح أزرق فاتح — تباين موحل وغير مريح للقراءة الطويلة (وقد عالجته جزئياً في شاشة المقال برفعه إلى `onSurface`، لكن المشكلة قائمة في بقية الشاشات).

**التوصية:** اعتماد أسطح **رمادية محايدة** (تميل لدفء خفيف جداً) + حبر داكن واحد عالي التباين + الأكسنت البرتقالي كلون علامة وحيد حاسم. الأزرق يُحتفظ به فقط كـ«ثانوي» للروابط/الحالات لا كلون سطح.

### 2) الطباعة — تدرّج ضعيف وأخطاء عربية
بعد توحيد الخط على Tajawal (قرار صحيح لإصلاح العربية)، فُقد التمايز بين العناوين والمتن: الكل عائلة واحدة بفروق وزن/حجم فقط. الصحف الرقمية تبني هيبتها على تدرّج طباعي قوي (Display ثقيل بمسافات حروف سالبة للعناوين، متن مريح بارتفاع سطر سخي).

أخطاء عربية حقيقية رصدتها:
- **`letterSpacing` على نص عربي** في عدة مواضع (شارة «عاجل» بـ `letterSpacing: 1`، تسميات الأقسام بـ `letterSpacing: 1.2`). العربية خط متّصل (cursive)؛ تباعد الحروف **يكسر الوصلات والليغاتprocessور** ويُشوّه الكلمة. يجب إزالته نهائياً من كل النصوص العربية.
- **`.toUpperCase()` على اسم القسم** في الكرت الأفقي — بلا أثر في العربية ومفهوم خاطئ مستورد من اللاتيني.
- ارتفاعات الأسطر غير موحّدة (1.3 / 1.4 / 1.8 / 1.95 عبر الشاشات).

**التوصية:** سلّم طباعي معياري (Modular Scale ~1.25)، وزن Display 800 للعناوين الكبيرة، متن 17px/ارتفاع 1.9، إزالة كل `letterSpacing` العربية، وتوحيد ارتفاعات الأسطر في `AppTypography`.

### 3) المكوّنات وحالات المحتوى — تشتّت واضح
الحالات الفارغة والخطأ **غير متّسقة إطلاقاً عبر الشاشات**:
- المحفوظات: حالة فارغة جميلة (دائرة + أيقونة + نص + وصف).
- الأكثر مشاهدة / الأقسام / البحث / الكاتب / عارض الصفحات: مجرد `Text(e.toString())` خام بلا أيقونة ولا تنسيق — يكشف رسائل تقنية للمستخدم.
- يوجد `AppErrorWidget` لكنه غير مستخدم في كل مكان، وأيقونته مثبّتة `wifi_off` لكل أنواع الأخطاء.

**التوصية:** مكوّنان موحّدان إلزاميان: `EmptyState` و`ErrorState` (أيقونة سياقية + عنوان + وصف + زر إجراء)، واستخدامهما في **كل** شاشة بلا استثناء.

### 4) الملاحة وهندسة المعلومات (IA)
الشريط السفلي يرفع **«البحث» كزر FAB مركزي** — أي يُعامَل كالإجراء الأساسي. في تطبيق أخبار، المحور يجب أن يكون «الرئيسية» أو «الأقسام»، والبحث إجراء ثانوي في الأعلى. هذا الاختيار يربك التسلسل ويهدر أهم موضع في الواجهة.

**التوصية:** إعادة ترتيب التبويبات: الرئيسية (محور) · الأقسام · الأكثر قراءة · المحفوظات · حسابي، ونقل البحث لأيقونة علوية ثابتة. أو إبقاء 5 تبويبات بلا رفع زر البحث.

### 5) أيقونات الاتجاه في RTL
استخدام `chevron_left` للدلالة على «التالي/الدخول» في الملف الشخصي والإعدادات والـ Drawer — **معكوس في RTL** (يجب `chevron_right` أو الاعتماد على `Directionality` وأيقونات `arrow_back_ios_new`). كذلك زر الرجوع غير موحّد: `arrow_forward_ios` في المقال مقابل الافتراضي في شاشات أخرى.

**التوصية:** توحيد أيقونة الرجوع، واعتماد أيقونات اتجاهية تحترم RTL تلقائياً.

### 6) التغذية الراجعة اللمسية والحركة
معظم العناصر القابلة للنقر تستخدم `GestureDetector` بلا أي تأثير ضغط (لا ripple، لا تظليل). النقر يبدو «ميتاً». ولا توجد انتقالات بين الشاشات ولا Hero transition من صورة الكرت إلى صورة المقال، ولا تلاشٍ من الـ Skeleton إلى المحتوى. الإحساس «الراقي» في تطبيقات الأخبار يأتي أساساً من هذه التفاصيل الحركية الدقيقة.

**التوصية:** استبدال `GestureDetector` بـ `InkWell`/`Material` حيث يناسب، إضافة `Hero` لصورة المقال، انتقالات صفحات ناعمة (fade/slide)، وتلاشٍ للمحتوى عند التحميل.

### 7) القيم الصلبة (Hardcoded) وغياب الترجمة
- ألوان داكنة مبعثرة (`#0D1B2A`، `#1A2840`، `#243552`) بدل توكنات.
- نصوص ثابتة مضلِّلة أو ينبغي أن تكون ديناميكية: وسوم «تريند» ثابتة في كود البحث، رقم الإصدار «1.0.0» مكتوب يدوياً، نصوص حالات التحميل في Splash.
- لا طبقة ترجمة (i18n)؛ كل النصوص مبعثرة في الودجِتات.

**التوصية:** ترقية توكنات الألوان لتشمل أسطح الوضع الداكن، جلب الإصدار من `package_info`، وبناء طبقة `l10n` ولو بسيطة لتجميع النصوص.

### 8) إمكانية الوصول (Accessibility)
- لا `Semantics` ولا أوصاف للصور أو الحقول.
- أهداف لمس صغيرة (أيقونات 16–18px) أقل من الحد الأدنى 48dp في عدة مواضع (مشاركة/حفظ في الكروت، chevrons).
- نصوص ثانوية بشفافية منخفضة (`secondary.withAlpha(0.6)`) قد تسقط في اختبار التباين WCAG AA.
- نقطة إيجابية: دعم تكبير الخط عبر `fontScaleProvider` موجود فعلاً.

**التوصية:** `Semantics` للعناصر التفاعلية والصور، رفع أهداف اللمس ≥48dp، ومراجعة التباين ليجتاز AA.

### 9) معالجة الصور والإيقاع البصري
نِسَب الصور غير موحّدة (كرت 160px ثابت، بطل 220، مقال 320) ومعالجات placeholder/خطأ مختلفة. لا «إيقاع» بصري متّسق بين البطاقات.

**التوصية:** نظام نِسَب موحّد (16:9 للأبطال، 4:3 أو 1:1 للكروت الأفقية)، ومكوّن صورة واحد بمعالجة placeholder/خطأ موحّدة.

### 10) ثغرات سلامة بيانات تمسّ التصميم
- البطاقة المميّزة في شاشة القسم تأخذ `items.first` دائماً بلا تحقق.
- رقائق الأقسام الفرعية (sub-category chips) تسميات ثابتة لا تُفلتر فعلياً.
- تحويلات `dynamic` بلا فحص null في شاشة الإشعارات قد تُفرغ العنوان/النص.

---

## ثالثاً: ملاحظات سريعة لكل شاشة

| الشاشة | أبرز ملاحظة تصميمية |
|--------|----------------------|
| Splash | جميلة، لكن مسافات صلبة (40/80) وزمن انتظار ثابت 3200ms؛ 300+ دائرة مرسومة قد تُثقل الأجهزة الضعيفة |
| الدخول/التسجيل | `crossAxisAlignment.start` غير مناسب لـ RTL؛ مؤشّر تحميل أبيض ثابت لا يناسب الوضع الفاتح |
| البحث | لون داكن صلب، وسوم «تريند» ثابتة، خطأ يُعرض كـ `toString()`، لا ripple |
| الأقسام | شبكة جيدة؛ تعتمد ألوان الأقسام من API (تحقّق من موثوقيتها)، حالة خطأ نص خام |
| القسم | بطاقة مميّزة قوية؛ لكن «خبر عاجل» نص ثابت، الرقائق الفرعية وهمية |
| الفيديو/الأكثر مشاهدة | حالات فارغة/خطأ خام بلا أيقونة؛ قيم alpha صلبة |
| المحفوظات | الأفضل اتساقاً (حالة فارغة مثالية) — يُحتذى بها كمعيار، ينقصها زر CTA للبحث |
| التعليقات | جيدة وظيفياً؛ مؤشّر تحميل أبيض ثابت، لا تحقق بصري لطول التعليق |
| الكاتب | نظيفة؛ خطأ خام، اسم افتراضي ثابت |
| الملف الشخصي | `chevron_left` معكوس RTL؛ Controllers لا تُفرّغ؛ لا حالة تحميل أثناء الحفظ |
| الإعدادات | الأفضل بنيةً (أقسام + عناوين)؛ لكن chevron معكوس، الإصدار ثابت، اللغة «معطّلة» بلا توضيح بصري |
| عارض الصفحات | بلا حشو داخلي للـ HTML، لا مؤشّر تمرير، RTL غير مضمون داخل HTML |
| الإشعارات | حذف فوري بلا تأكيد؛ تحويلات dynamic خطرة |
| Drawer | `chevron_left` معكوس؛ خلط بين `go`/`push`؛ بلا semantics |
| Skeleton | ألوان داكنة صلبة؛ لا نِسَب صور؛ `cardTheme.color` بلا fallback |

---

## رابعاً: خطة التطوير على مراحل

### المرحلة 1 — أساس اللغة البصرية (أعلى أثر)
1. **إعادة بناء لوحة الألوان:** أسطح رمادية محايدة، حبر `#15181C` تقريباً، أكسنت برتقالي وحيد، أزرق ثانوي للروابط فقط. تحديث `AppColors` ووضعي الداكن/الفاتح بالكامل، وإزالة كل اللون الصلب.
2. **سلّم طباعي محترف:** إعادة كتابة `AppTypography` بمقاييس Modular، أوزان Display، ارتفاعات أسطر موحّدة، و**إزالة كل `letterSpacing` و`.toUpperCase()` العربية**.
3. **توكنات مسافات منضبطة:** فرض شبكة 4-pt واستبدال القيم الخام (12/20/32) بثوابت `AppSpacing`.

### المرحلة 2 — توحيد المكوّنات
4. مكوّنات موحّدة: `EmptyState`، `ErrorState`، `AppImage` (نِسَب + placeholder موحّد)، `PressableCard` (مع ripple). تطبيقها في كل الشاشات.
5. توحيد أيقونات الاتجاه (RTL) وزر الرجوع.
6. ترقية الـ Skeleton لتوكنات + نِسَب صور صحيحة.

### المرحلة 3 — تجربة وحركة
7. `Hero` لصورة المقال (كرت ← تفصيل)، انتقالات صفحات ناعمة، تلاشي المحتوى بعد التحميل.
8. تغذية راجعة لمسية (ripple + haptics خفيف عند الحفظ/التبديل).
9. إعادة ترتيب الشريط السفلي (إنزال البحث للأعلى، الرئيسية محوراً).
10. حالات «مقروء/محفوظ» مرئية على الكروت.

### المرحلة 4 — جودة واحترافية
11. إمكانية الوصول: `Semantics`، أهداف لمس ≥48dp، تباين AA.
12. طبقة ترجمة `l10n` وجلب الإصدار ديناميكياً.
13. سلامة البيانات: فحوص null، إزالة التسميات الوهمية، البطاقة المميّزة آمنة.

---

## خامساً: توكنات مقترحة (مرجع تنفيذي)

```text
// أسطح محايدة (فاتح)
bg        #FAFAF9   surface     #FFFFFF   surfaceAlt #F4F4F2
border    #E7E7E4   ink         #16181C   inkSoft    #5B6068
// أسطح (داكن)
bgD       #0B0E12   surfaceD    #14181E   surfaceAltD #1B212A
borderD   #232A33   inkD        #ECEEF1   inkSoftD   #9AA3AE
// العلامة
accent    #F0640C   accentDark  #B84A06   accentSoft #FCE9DC
// دلالات
breaking  #E11D48   live #16A34A   link #2563C9
```

```text
// السلّم الطباعي (Tajawal)
display   34 / 800 / 1.25
h1        26 / 800 / 1.30
h2        21 / 700 / 1.35
title     18 / 700 / 1.40
body      17 / 400 / 1.90   (قراءة المقال)
bodySm    15 / 400 / 1.70
label     13 / 600 / 1.40   (بدون letterSpacing)
caption   11 / 500 / 1.30
```

```text
// المسافات (4-pt)
4 · 8 · 12 · 16 · 20 · 24 · 32 · 40 · 56
// أنصاف الأقطار
sm 8 · md 12 · lg 16 · xl 20 · pill 999
```

---

## سادساً: مؤشّرات النجاح
- اجتياز تباين WCAG AA لكل النصوص.
- صفر نصوص تقنية (`toString()`) معروضة للمستخدم.
- صفر `letterSpacing`/`toUpperCase` على نص عربي.
- مكوّن واحد فقط لكل من: الحالة الفارغة، الخطأ، الصورة، الكرت القابل للنقر.
- زمن إدراك أسرع عبر Hero + تلاشي المحتوى.

> الأولوية القصوى: **المرحلة 1** (الألوان + الطباعة) لأنها ترفع الإحساس العام للتطبيق فوراً وتؤسّس لكل ما بعدها. جاهز لبدء التنفيذ فور موافقتك، مرحلةً مرحلة مع بناء وتحقّق بعد كل مرحلة.
