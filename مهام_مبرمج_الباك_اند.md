# مهام مطلوبة على خادم WordPress (إضافة Almadar Mobile API)

التطبيق (Flutter) جاهز ويستدعي هذه الأشياء فعليًا. المطلوب منك إكمال طرف الخادم في مهمّتين فقط.

**معلومات عامة:**
- الـ Namespace: `almadar/v1`
- الموقع يستخدم Permalinks عادية، فصيغة الاستدعاء: `https://almadar-news.com/?rest_route=/almadar/v1/...`
- المصادقة: التطبيق يرسل ترويسة `Authorization: Bearer <token>` (نفس التوكن الصادر من `auth/login`).
- شكل الاستجابة المتوقّع: النجاح `{ "data": {...} }` / الخطأ `{ "error": { "message": "..." } }`.

---

## المهمة 1: نقطة حذف الحساب (إلزامية لسياسة Google Play)

**Endpoint:** `POST /almadar/v1/auth/delete`
- الترويسة: `Authorization: Bearer <token>`
- بدون body.
- الوظيفة: حذف المستخدم صاحب التوكن **نهائيًا** مع كل بياناته (تعليقات/محفوظات… إلخ).
- النجاح: أعِد `200` مع `{ "data": { "deleted": true } }`.
- الفشل/غير مصرّح: أعِد `401/500` مع `{ "error": { "message": "..." } }`.

### كود جاهز (ضعه في ملف الإضافة):
```php
add_action('rest_api_init', function () {
    register_rest_route('almadar/v1', '/auth/delete', array(
        'methods'  => 'POST',
        'callback' => 'almadar_delete_account',
        // نفس آلية التحقق المستخدمة في auth/me — يجب أن يكون المستخدم مسجّلًا:
        'permission_callback' => function () {
            return get_current_user_id() > 0;
        },
    ));
});

function almadar_delete_account() {
    $user_id = get_current_user_id();

    if ($user_id <= 0) {
        return new WP_REST_Response(
            array('error' => array('message' => 'غير مصرّح')), 401
        );
    }

    // لا تسمح بحذف حسابات الإدارة عن طريق الخطأ.
    $user = get_userdata($user_id);
    if ($user && in_array('administrator', (array) $user->roles, true)) {
        return new WP_REST_Response(
            array('error' => array('message' => 'لا يمكن حذف حساب إداري')), 403
        );
    }

    require_once ABSPATH . 'wp-admin/includes/user.php';

    // احذف المستخدم وكل محتواه (reassign = null يحذف مقالاته أيضًا).
    $deleted = wp_delete_user($user_id, null);

    if (!$deleted) {
        return new WP_REST_Response(
            array('error' => array('message' => 'تعذّر حذف الحساب')), 500
        );
    }

    return new WP_REST_Response(array('data' => array('deleted' => true)), 200);
}
```

> ملاحظة: إذا كان الـ JWT لديك **لا يملأ المستخدم الحالي تلقائيًا** في `get_current_user_id()`،
> استخدم نفس دالة التحقق/فكّ التوكن المستخدمة في `auth/me` لاستخراج `user_id` من الترويسة،
> واستبدل بها السطر `$user_id = get_current_user_id();`.

### اختبار سريع:
```bash
curl -X POST "https://almadar-news.com/?rest_route=/almadar/v1/auth/delete" \
  -H "Authorization: Bearer <TOKEN>"
# المتوقع: {"data":{"deleted":true}}
```

**مطلوب أيضًا (Play Console):** صفحة ويب عامة لحذف الحساب (رابط يفتحه المستخدم دون تثبيت التطبيق).

---

## المهمة 2: إضافة المصدر الأصلي للأخبار المنقولة

التطبيق يقرأ حقلين من كل مقال في مخرجات الـ API (endpoints: `posts`, `posts/{id}`, `trending`, `most-viewed`, `videos`, `search`, `authors/{id}/posts`):

- `source` : اسم المصدر (نص). مثال: `"وكالة الأنباء"`.
- `source_url` : رابط الخبر الأصلي (نص، اختياري).

يقبل التطبيق أيضًا الشكل الكائني: `"source": { "name": "...", "url": "..." }`.

### الخطوة أ — مكان يُدخل فيه المحرّر المصدر (Meta Box):
```php
// حقلان في صفحة تحرير المقال.
add_action('add_meta_boxes', function () {
    add_meta_box('almadar_source', 'مصدر الخبر (للمنقول)',
        'almadar_source_box', 'post', 'side');
});

function almadar_source_box($post) {
    $name = get_post_meta($post->ID, '_almadar_source', true);
    $url  = get_post_meta($post->ID, '_almadar_source_url', true);
    wp_nonce_field('almadar_source_save', 'almadar_source_nonce');
    echo '<p><label>اسم المصدر</label><input type="text" name="almadar_source" '
        . 'value="' . esc_attr($name) . '" style="width:100%"></p>';
    echo '<p><label>رابط المصدر</label><input type="url" name="almadar_source_url" '
        . 'value="' . esc_attr($url) . '" style="width:100%"></p>';
}

add_action('save_post', function ($post_id) {
    if (!isset($_POST['almadar_source_nonce']) ||
        !wp_verify_nonce($_POST['almadar_source_nonce'], 'almadar_source_save')) return;
    if (defined('DOING_AUTOSAVE') && DOING_AUTOSAVE) return;
    if (!current_user_can('edit_post', $post_id)) return;

    update_post_meta($post_id, '_almadar_source',
        sanitize_text_field($_POST['almadar_source'] ?? ''));
    update_post_meta($post_id, '_almadar_source_url',
        esc_url_raw($_POST['almadar_source_url'] ?? ''));
});
```

### الخطوة ب — أضف الحقلين إلى مخرجات المقال في الـ API:
في **الدالة التي تبني مصفوفة المقال** داخل إضافتك (نفس المكان الذي تُخرِج فيه `title`, `excerpt`, `author`… إلخ)، أضِف هذين السطرين:
```php
'source'     => get_post_meta($post->ID, '_almadar_source', true) ?: '',
'source_url' => get_post_meta($post->ID, '_almadar_source_url', true) ?: '',
```
> استبدل `$post->ID` بمُعرّف المقال حسب متغيّرك في تلك الدالة.

### اختبار سريع:
```bash
curl "https://almadar-news.com/?rest_route=/almadar/v1/posts/<POST_ID>"
# تأكّد من ظهور "source" و "source_url" في الاستجابة.
```

---

## الخلاصة
| # | المهمة | الحالة على التطبيق |
|---|--------|------------------|
| 1 | `POST auth/delete` | ✅ التطبيق يستدعيها ويمسح الجلسة تلقائيًا |
| 2 | `source` + `source_url` في مخرجات المقال | ✅ التطبيق يقرؤهما ويعرض "المصدر" تلقائيًا |

بمجرد إنجاز هاتين المهمّتين يصبح طرف الخادم مكتملًا ومتوافقًا مع سياسة News & Magazines.
