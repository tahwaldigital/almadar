# مهام المبرمج — تطبيق المدار نيوز (Flutter)

> **خلفية:** الـ Backend (WordPress / إضافة `almadar/v1`) **جاهز ومختبَر بالكامل** للنقاط أدناه. المطلوب منك **واجهة التطبيق + Firebase + البناء + رفع Play Console**. لا تعدّل السيرفر.

**Base URL (إلزامي بالـ www):**
```
https://www.almadar-news.com/?rest_route=/almadar/v1
```
> مهم جدًا: استخدم **www**. النطاق بدون www يعمل تحويل 301 يكسر طلبات POST (تسجيل/دخول/تعليق). (تم ضبطه في `lib/core/constants/api_constants.dart`.)

كل الاستجابات بالشكل: `{ "success": true, "data": {...} }` — والأخطاء: `{ "success": false, "error": { "code", "message" } }`.

---

## المهمة 1 — قسم التعليقات في شاشة المقال (أولوية عالية)

أضف قسم تعليقات أسفل محتوى المقال في `news_detail_screen.dart` (عرض + إضافة).

**عرض التعليقات:**
```
GET /comments?post={POST_ID}&page=1&per_page=20
```
يرجّع `data` = مصفوفة تعليقات + `meta.pagination`. شكل التعليق:
```json
{ "id":123, "post_id":18900, "parent":0, "author_name":"...",
  "author_avatar":"https://...", "content":"<p>...</p>", "date":"2026-...",
  "date_human":"منذ ساعة" }
```
> `content` يأتي كـ HTML — اعرضه عبر `HtmlWidget` (مستخدم بالفعل في المشروع).

**إضافة تعليق:**
```
POST /comments
Body: { "post": POST_ID, "content": "نص التعليق", "parent": 0 }
```
- لو المستخدم مسجّل دخول: أرسل `Authorization: Bearer <token>` (يُنسب التعليق لحسابه تلقائيًا).
- لو ضيف: أرسل `author_name` (إلزامي) + `author_email` (اختياري).
- الاستجابة: `{ data: { comment:{...}, approved:bool, message:"..." } }`.
  - لو `approved=false` اعرض رسالة «تم استلام تعليقك وهو قيد المراجعة».

**المطلوب في الواجهة:**
1. قسم «التعليقات (العدد)» أسفل المقال، قائمة بالتعليقات (صورة + اسم + وقت + نص) مع Pagination/تحميل المزيد.
2. صندوق كتابة تعليق: **إن لم يكن مسجّلًا** اعرض زر «سجّل الدخول للتعليق» يوجّه لشاشة الدخول؛ إن كان مسجّلًا اعرض حقل النص + زر إرسال.
3. بعد الإرسال حدّث القائمة (أو أضف التعليق أعلى القائمة إن `approved`).
4. حالات فارغة/تحميل/خطأ (استخدم `EmptyState`/`ErrorState` الموجودين).

**معايير القبول:** أكتب تعليقًا كمستخدم مسجّل → يظهر في القائمة؛ الضيف يُوجَّه للدخول؛ لا أخطاء Console.

---

## المهمة 2 — شاشة تأكيد البريد بعد التسجيل (أولوية عالية)

الـ Backend يرسل الآن **كود 6 أرقام** ببريد HTML احترافي عند التسجيل، ويضيف `email_verified` لبيانات المستخدم.

**التدفّق المطلوب:**
1. بعد نجاح `POST /auth/register` → ادخل التطبيق لكن **وجّه المستخدم لشاشة «تأكيد البريد»** (استجابة التسجيل تحتوي `data.user.email_verified = false`).
2. شاشة التأكيد: حقل إدخال كود 6 أرقام + زر «تأكيد» + زر «إعادة إرسال الكود» (عطّله 60 ثانية بعد كل إرسال).

**تأكيد الكود:**
```
POST /auth/verify-email
Body: { "email": "<user email>", "code": "123456" }
→ نجاح: { data: { verified:true, user:{...} } }
→ خطأ 400: كود غير صحيح / منتهي (اعرض error.message)
```
**إعادة إرسال:**
```
POST /auth/resend-code
Body: { "email": "<user email>" }
→ { data: { sent:true } }   (يرفض إن أقل من 60 ثانية: 429)
```
3. عند `verified:true` → أكمل للتطبيق (المستخدم مسجّل بالفعل).
4. **غير حاجب:** الدخول ينجح حتى لو غير مؤكَّد؛ لو `email_verified=false` بعد الدخول اعرض تنبيه/بانر «فعّل بريدك» يفتح شاشة التأكيد. (قرار المالك: هل نمنع التعليق/التنبيهات حتى التأكيد؟ — نفّذ حجب التعليق للمستخدم غير المؤكَّد إن طُلب.)

**معايير القبول:** سجّل حساب جديد → تصل رسالة بها كود → أدخله → يتأكد الحساب.

---

## المهمة 3 — Firebase لتفعيل التنبيهات الفعلية (أولوية متوسطة)

الكود في التطبيق جاهز (اشتراك في `news_all` + `breaking` + مفاتيح التنبيهات + تسجيل التوكن)، لكن **Firebase غير مُعدّ**:
1. أنشئ مشروع Firebase، أضف تطبيق Android بحزمة **`almadar.news`** (و iOS لو مطلوب).
2. نزّل `google-services.json` → `android/app/`، وشغّل `flutterfire configure` لتوليد `lib/firebase_options.dart`، وحدّث `main.dart` لاستخدام `DefaultFirebaseOptions` عند `Firebase.initializeApp`.
3. سلّم **Service Account (JSON)** لمالك الموقع لرفعه في لوحة ووردبريس (Almadar App → Push) لتفعيل الإرسال من السيرفر.

> بدون هذه الخطوة: كل شيء يعمل عدا وصول الإشعار الفعلي للجهاز.

---

## المهمة 4 — البناء ورفع Google Play (أولوية عالية)

1. النسخة الحالية `1.0.1+2` في `pubspec.yaml` — ارفع الرقم عند كل رفع جديد.
2. بناء حزمة موقّعة:
   ```
   flutter build appbundle --release
   ```
   (التوقيع مضبوط: `android/key.properties` + `android/app/almadar-news-release.jks`.)
3. ارفع `build/app/outputs/bundle/release/app-release.aab`.
4. في Play Console (يعملها المالك):
   - **Store listing → Store listing contact details:** الموقع `https://www.almadar-news.com/` + بريد `info@almadar-news.com` + هاتف `+966549140787`.
   - **نموذج إقرار News & Magazines → رابط الاتصال:** `https://www.almadar-news.com/?page_id=18907`
   - تأكيد الفئة = News & Magazines.

---

## ملاحظات (تم إنجازه بالفعل — لا تكرره)
- ✅ إصلاح فشل التسجيل/الدخول (تحويل www) — في `api_constants.dart`.
- ✅ إعادة تصميم شاشة الأقسام كقائمة — `categories_screen.dart`.
- ✅ اسم الكاتب = قيمة حقل «اسم الكاتب» في محرر ووردبريس (سيرفر).
- ✅ عرض مصدر الخبر في الموقع والتطبيق (سيرفر + `_SourceAttribution`).
- ✅ تحديد «خبر هام» = صندوق «Mark as Breaking News» في محرر المقال.

## عقود API إضافية جاهزة (للمرجع)
- `POST /auth/login` { username|email, password }
- `POST /auth/register` { name, email, password }
- `POST /auth/delete` (Bearer، بلا body) → حذف الحساب نهائيًا
- `POST /devices/register` { token, platform, topics[] }
- كل مسارات المحتوى: `/posts`, `/posts/{id}`, `/categories`, `/breaking-news`, `/trending`, `/most-viewed`, `/search`, `/videos`, `/authors/{id}`, `/pages`.
