# تقرير فني احترافي — مشاكل تطبيق «المدار نيوز» والربط مع الموقع + تدقيق UI/UX

> تاريخ الفحص: 2026-06-01 · الفاحص: Claude Code · النطاق: تطبيق Flutter + الربط بـ API الموقع (`almadar/v1`) + تجربة الاستخدام والتصميم
> طبيعة التقرير: **تشخيص فقط — لم يتم تنفيذ أي إصلاح** (بناءً على طلبك)

---

## 0) الخلاصة التنفيذية (TL;DR)

| البند | النتيجة | التفصيل |
|------|---------|---------|
| الـ API على الموقع | **سليم تماماً** | اختُبر حياً: `/posts`، `/posts/{id}`، `/categories`، إلخ كلها ترجع 200 |
| محتوى المقال يرجع من الموقع؟ | **نعم** | `/posts/14939` يرجع `content` بـ ~4500 حرف HTML داخل غلاف `{success,data}` |
| التصنيفات ترجع من الموقع؟ | **نعم** | 14 تصنيفاً، كلها `hidden=false` |
| **السبب الجذري لعدم ظهور المحتوى** | **في التطبيق (طرف العميل)** | ثلاث طبقات متراكبة (تفصيلها في القسم 3) |
| **أخطر اكتشاف** | **نُسختان متضاربتان من التطبيق** | يتم بناء النسخة **القديمة** بينما كل الإصلاحات في نسخة **أخرى** |

**الزُبدة بجملة واحدة:** المشكلة ليست في الموقع ولا في الـ API. المشكلة أنك تبني وتشغّل نسخة قديمة من التطبيق فيها ثلاثة أعطال في طبقة عرض المحتوى، بينما النسخة المُصلَحة موجودة في مجلد مختلف ولا يتم بناؤها.

---

## 1) الاكتشاف الحاسم: نسختان مختلفتان من التطبيق

يوجد على جهازك نسختان من نفس المشروع، وهما **غير متطابقتين**:

| | النسخة القديمة (التي تُبنى) | النسخة المُصلَحة (المهملة) |
|---|---|---|
| المسار | `C:\dev\almadar_news` | `C:\Users\pc\Music\WEB\ابلكيشن المدار\almadar_news` |
| مكتبة عرض HTML | `flutter_html: ^3.0.0-beta.2` (مهملة) | `flutter_widget_from_html_core: ^0.16.1` (مُصانة) |
| عدد الشاشات | 8 مجلدات شاشات | 16 مجلد شاشات |
| شاشات ناقصة فيها | لا يوجد: author / categories / comments / most_viewed / notifications / page / profile / videos | كل الشاشات موجودة |
| آخر تعديل للكود | 2026-05-21 | 2026-05-22 (أحدث) |
| **آخر عملية بناء (build/)** | **2026-06-01 (اليوم!)** | — |

**الدليل القاطع:** مجلد `build/` داخل `C:\dev\almadar_news` تعديله بتاريخ **اليوم**، أي أنك تبني هذه النسخة فعلياً — وهي القديمة الناقصة. كل التحسينات (استبدال مكتبة HTML، الشاشات الجديدة، تجديد التوكن، إلخ) طُبّقت على نسخة Music ولم تصل إلى النسخة التي تشغّلها.

> هذا وحده يفسّر «التطبيق لا يظهر المحتوى» و«التصنيفات لا تظهر» معاً.

**قرار مطلوب منك (مهم قبل أي إصلاح):** أيّ النسختين هي المعتمدة؟ التوصية: اعتماد **نسخة واحدة فقط** وحذف/أرشفة الأخرى لتفادي تكرار هذا الالتباس. (الأنسب تقنياً: اعتماد نسخة Music لأنها الأحدث والأكمل، أو نقل إصلاحاتها إلى `C:\dev`.)

---

## 2) حالة الـ API والربط (مُختبَرة حياً)

استُدعيت نقاط الـ API الحقيقية على `https://almadar-news.com` بنفس الصيغة التي يبنيها التطبيق (`?rest_route=/almadar/v1/...`):

| النقطة | الكود | الملاحظات |
|--------|-------|-----------|
| `/posts?per_page=2` | 200 | غلاف `{success, data, meta}` — قائمة سليمة |
| `/posts/{id}` | 200 | **يحتوي `content`** (~4500 حرف HTML) |
| `/categories` | 200 | **14 تصنيفاً**، بكل تصنيف `count` و`hidden` |

**ملاحظة بنيوية مهمة (ليست عطباً — بل تصميم مقصود):**
نقطة القائمة `/posts` **لا تُرجع حقل `content`** (لتخفيف حجم الاستجابة). الحقول الراجعة في القائمة: `id, slug, title, excerpt, date, author, primary_category, categories, tags, featured_image, views, reading_time, comment_count, is_breaking, is_video` — بدون `content`. المحتوى الكامل يُجلب فقط عبر `/posts/{id}`.

➡️ **هذه النقطة بالذات هي مفتاح فهم العطل الأول في التطبيق** (القسم 3).

**ملاحظة على الاتصال بـ MCP:** في هذه الجلسة لا يظهر خادم MCP خاص بـ«المدار» ضمن الأدوات المتصلة (تظهر مواقع Novamira الأخرى فقط). لذلك تم فحص جانب الموقع عبر طلبات HTTP عامة حقيقية (وهي كافية لإثبات سلامة الـ API)، وليس عبر قراءة كود الإضافة مباشرة هذه المرة. إن أردت تدقيقاً لكود الإضافة نفسه (PHP) أحتاج إعادة تفعيل اتصال MCP الخاص بالموقع.

---

## 3) السبب الجذري لعدم ظهور محتوى المقال (ثلاث طبقات متراكبة)

العطل ليس سبباً واحداً، بل ثلاثة عيوب في النسخة القديمة، أي منها كافٍ لإفراغ المحتوى:

### الطبقة 1 — شاشة المقال لا تجلب المقال الكامل أصلاً (الأخطر)
في [news_detail_screen.dart](lib/presentation/screens/news_detail/news_detail_screen.dart):
- السطر 60: `final article = widget.article;` — تعتمد على الكائن المُمرَّر من القائمة.
- السطر 243: `Html(data: article.content, ...)` — تعرض محتوى الكائن المُمرَّر.
- الشاشة **لا تستدعي أبداً** `getArticleById` لجلب النسخة الكاملة.

وبما أن القائمة `/posts` لا ترجع `content` (القسم 2)، فإن `article.content` = **سلسلة فارغة دائماً** → منطقة المحتوى فارغة بالكامل، بينما العنوان والصورة (الموجودان في القائمة) يظهران.

> ملاحظة: طبقة البيانات نفسها سليمة — [news_remote_datasource.dart:149](lib/data/datasources/remote/news_remote_datasource.dart) فيها `getArticleById` صحيحة وتفك الغلاف `data` جيداً. العطب أن **الشاشة لا تستعملها**.

### الطبقة 2 — مكتبة عرض HTML مهملة
`pubspec.yaml` يستخدم `flutter_html: ^3.0.0-beta.2` — نسخة تجريبية مهجورة منذ سنوات، تسبب فراغاً أو استثناءات عرض صامتة في إصدارات Flutter الحديثة (خصوصاً في وضع release). حتى لو وصل المحتوى، قد لا يُعرض.

### الطبقة 3 — Build قديم على الجهاز
حتى لو صُلِّحت الطبقتان أعلاه، تغيير الحزم وطبقة البيانات يتطلب **بناءً نظيفاً** (`flutter clean` + إعادة تثبيت)، لأن hot reload لا يكفي.

**النتيجة المركّبة:** المحتوى الكامل لا يُطلب (طبقة 1)، وحتى لو طُلب فقد لا يُعرض (طبقة 2)، وحتى لو صُلِّح فلن يظهر بدون بناء نظيف (طبقة 3).

---

## 4) سبب عدم ظهور التصنيفات

في [app_router.dart:45-49](lib/presentation/router/app_router.dart) مسار `/categories` يبني `SearchScreen`:
```dart
GoRoute(path: '/categories', name: 'categories',
        builder: (context, state) => const SearchScreen()), // ← خطأ
```
وفي [main_scaffold.dart:12](lib/presentation/widgets/main_scaffold.dart) تبويب «الأقسام» في الشريط السفلي يفتح `/categories`. فعند الضغط على «الأقسام» تفتح **شاشة البحث** بدل شبكة الأقسام → تبدو التصنيفات «غير موجودة». (النسخة القديمة لا تملك `CategoriesScreen` أصلاً.)

في الرئيسية يوجد شريط تصنيفات أفقي ([category_tabs](lib/presentation/screens/home/widgets/category_tabs.dart))، لكنه يحتاج تمريراً أفقياً لرؤية الـ14 تصنيفاً، وقد يبدو ناقصاً.

---

## 5) كتالوج الأعطال (مرتّب حسب الخطورة)

### حرجة (تمنع الوظيفة الأساسية)
| # | العطل | الموقع | الأثر |
|---|------|--------|-------|
| C1 | شاشة المقال تعرض محتوى الكائن المُمرَّر بدل جلب الكامل بالـ id | news_detail_screen.dart:60,243 | لا محتوى للمقال إطلاقاً |
| C2 | مكتبة `flutter_html` beta مهملة | pubspec.yaml | فراغ/استثناء عرض |
| C3 | تبويب «الأقسام» يفتح شاشة البحث | app_router.dart:47 | التصنيفات لا تظهر كقسم |
| C4 | النسخة المبنية ≠ النسخة المُصلَحة | (مستوى المشروع) | كل الإصلاحات لا تظهر |

### متوسطة (وظائف ناقصة/أزرار معطّلة)
| # | العطل | الموقع |
|---|------|--------|
| M1 | زر القائمة (Drawer) في الرئيسية لا يفعل شيئاً | home_screen.dart:89-91 (`onPressed: () {}`) |
| M2 | زر الإشعارات في الرئيسية لا يفعل شيئاً | home_screen.dart:107-111 |
| M3 | زر الإشعارات في شاشة المقال لا يفعل شيئاً | news_detail_screen.dart:94-98 |
| M4 | زر «تعليق» في شاشة المقال لا يفعل شيئاً | news_detail_screen.dart:386-388 |
| M5 | لا يوجد تجديد توكن عند 401 (الجلسة تنتهي صامتة) | dio_client.dart:92 (يرمي خطأ فقط) |
| M6 | شاشات مفقودة كلياً: تعليقات، فيديو، الكاتب، الملف الشخصي، مركز الإشعارات، الصفحات الثابتة، الأكثر مشاهدة | (غير موجودة في النسخة القديمة) |

### منخفضة (تحسينات/تنظيف)
| # | العطل | الموقع |
|---|------|--------|
| L1 | حالة `_isHeaderVisible` تُحسب ولا تُستخدم (كود ميت) | home_screen.dart:20,36-43 |
| L2 | `statusBarIconBrightness` مثبّت `dark` → أيقونات شريط الحالة تختفي في الوضع الداكن | main.dart:56 |
| L3 | `main.dart` ينشئ `DioClient()` جديداً يدوياً بدل مزوّد Riverpod | main.dart:106 |
| L4 | إشعارات FCM لن تعمل قبل إدخال Service Account JSON في إعدادات الإضافة | (إعداد على الموقع) |
| L5 | الدخول الاجتماعي: الـ backend جاهز (`/auth/social`) لكن واجهة Google/Apple غير مضافة | (ناقص) |

---

## 6) تدقيق تجربة الاستخدام والتصميم (UI/UX)

### نقاط القوة
- بنية معمارية نظيفة (Clean Architecture + Riverpod + go_router).
- استخدام Skeleton/Shimmer أثناء التحميل، و`RefreshIndicator` للسحب للتحديث، ودعم الوضع الداكن.
- تدرّجات وصور بطل (Hero) وتنسيق RTL في شاشة المقال.

### مشاكل التصميم والتجربة
| # | الملاحظة | الخطورة | التفصيل |
|---|---------|---------|---------|
| UX1 | **الخطوط لاتينية للنص العربي** | عالية | [app_typography.dart](lib/core/constants/app_typography.dart) يستخدم Rubik / Work Sans / IBM Plex Sans لكل النصوص. هذه خطوط لاتينية بلا محارف عربية مناسبة → Flutter يسقط على خط النظام الافتراضي، فتظهر العربية بشكل غير متّسق وغير محترف. التوصية: خط عربي مخصص (Tajawal / IBM Plex Sans Arabic / Cairo) عبر `google_fonts` أو أصول محلية. |
| UX2 | أزرار «ميتة» تعطي انطباع تطبيق غير مكتمل | عالية | القائمة + الإشعارات + التعليق (M1-M4) تستجيب بصرياً لكن لا تفعل شيئاً — أسوأ من إخفائها. |
| UX3 | لا حالة فارغة/خطأ واضحة في شاشة المقال عند فراغ المحتوى | متوسطة | حين يكون `content` فارغاً لا تظهر رسالة؛ فقط فراغ صامت يربك المستخدم. |
| UX4 | الشريط السفلي: التبويب الأوسط (بحث) مرفوع كزر FAB، ما يوحي بأنه الإجراء الأساسي بينما هو ثانوي | متوسطة | [main_scaffold.dart:58](lib/presentation/widgets/main_scaffold.dart) — مراجعة هرمية التنقّل (الأنسب جعل «الرئيسية» أو «الأقسام» محور الانتباه). |
| UX5 | زر الرجوع يستخدم `arrow_forward_ios` | منخفضة | news_detail_screen.dart:85 — مقبول في RTL لكن يُفضّل أيقونة رجوع دلالية موحّدة. |
| UX6 | أيقونات شريط الحالة تختفي في الوضع الداكن | منخفضة | L2 أعلاه. |
| UX7 | حاوية بيضاء ثابتة خلف اللوجو في كل الأوضاع | منخفضة | home_screen.dart:98 — يكسر تناسق الوضع الداكن؛ يُفضّل نسخة لوجو فاتحة للوضع الداكن. |

---

## 7) خطة الإصلاح المقترحة (بالأولوية) — للتنفيذ بعد موافقتك

**المرحلة 0 — حسم النسخة (إلزامية أولاً):**
1. تحديد النسخة المعتمدة (توصية: Music أو نقل إصلاحاتها إلى `C:\dev`)، وحذف/أرشفة الأخرى.

**المرحلة 1 — إعادة ظهور المحتوى (حرجة):**
2. جعل شاشة المقال تجلب الكامل عبر `getArticleById` / `articleByIdProvider` مع حالات تحميل/خطأ.
3. استبدال `flutter_html` بـ `flutter_widget_from_html_core`.
4. تصحيح مسار `/categories` ليفتح شاشة أقسام حقيقية (شبكة الـ14 تصنيفاً).
5. بناء نظيف: `flutter clean && flutter pub get` + إلغاء تثبيت النسخة القديمة من الجهاز.

**المرحلة 2 — الوظائف الناقصة (متوسطة):**
6. ربط الأزرار المعطّلة (Drawer، الإشعارات، التعليق) أو إخفاؤها مؤقتاً.
7. إضافة تجديد التوكن عند 401.
8. استكمال الشاشات الناقصة (إن اعتُمدت نسخة `C:\dev`).

**المرحلة 3 — UI/UX (تحسينات):**
9. اعتماد خط عربي مخصص.
10. حالات فارغة/خطأ، مراجعة هرمية الشريط السفلي، إصلاح شريط الحالة في الوضع الداكن، لوجو الوضع الداكن.

**المرحلة 4 — تكامل متقدم (اختياري):**
11. تفعيل FCM (Service Account)، الدخول الاجتماعي، التفكير في Pretty Permalinks + `/wp-json`.

---

## 8) ملاحظة منهجية
- كل اختبارات الـ API أُجريت عبر طلبات HTTP حقيقية على `almadar-news.com`.
- كل إشارات `file:line` تخص النسخة قيد البناء (`C:\dev\almadar_news`).
- لم تُجرَ أي تعديلات على الكود أو الموقع — هذا تقرير تشخيصي بحت كما طلبت.
